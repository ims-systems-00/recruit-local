export interface IExperience {
  company: string;
  location?: string;
  workMode?: string;
  employmentType?: string;
  position: string;
  startDate: Date;
  endDate?: Date;
  description?: string;
}
